{
  "name": "@giggle-academy/bridge",
  "version": "1.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "description": "Modern JavaScript Bridge for Web-Native communication",
  "keywords": ["bridge", "native", "webview", "ios", "android"],
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.0.0"
  }
}
